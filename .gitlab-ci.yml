stages:
  - build


build_project:
  stage: build
  script:
    - xcodebuild clean -workspace NoLo-AD.xcworkspace -scheme SchemeName | xcpretty
    - xcodebuild test -workspace NoLo-AD.xcworkspace -scheme SchemeName | xcpretty -s
 